import Server from "../Server";

export default class Http {
  //   uri: string;
  constructor() {
    this.getHttpServer();
  }
  getHttpServer() {
    //get server psql()
    // store in redis
    // ping server
  }
}
